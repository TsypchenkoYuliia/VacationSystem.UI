export const getType = (id) => {
    if (id === 1)
        return 'Administrative'
    else if (id === 2)
        return 'Annual'
    else if (id === 3)
        return 'Study'
    else if (id === 4)
        return 'Sick'
};